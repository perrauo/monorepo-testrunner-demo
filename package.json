{
    "name": "monorepo-testrunner-demo",
    "private": true,
    "scripts": {
        "bootstrap": "lerna bootstrap",
        "test": "lerna run test"
    },
    "devDependencies": {
        "lerna": "^8.1.3"
    }
}
